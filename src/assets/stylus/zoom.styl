.zoom-enter-active
  opacity 0
  transform scale(.95)
  transition opacity .15s ease-out, transform .1s ease
.zoom-enter-to
  transform scale(1)
  opacity 1

.zoom-leave-active
  transition .15s ease
  opacity 1
  transform scale(1)
.zoom-leave-to
  transform scale(.98)
  opacity 0