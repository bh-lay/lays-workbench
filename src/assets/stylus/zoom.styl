.zoom-enter-active
  opacity 0
  transform scale(.95)
  transition opacity .2s ease-out, transform .15s ease
.zoom-enter-to
  transform scale(1)
  opacity 1

.zoom-leave-active
  transition .2s ease-out
  opacity 1
  transform scale(1)
.zoom-leave-to
  transform scale(.95)
  opacity 0